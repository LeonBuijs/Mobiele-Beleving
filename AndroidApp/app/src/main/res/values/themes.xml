<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Cobra" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/green</item>
        <item name="android:windowBackground">@color/darkGreen</item>
        <item name="textAppearanceButton">@color/black</item>
    </style>

    <style name="JohanEnDeEenhoorn" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple</item>
        <item name="recyclerViewStyle">@color/pink</item>
        <item name="android:windowBackground">@color/yellow</item>
        <item name="textAppearanceButton">@color/black</item>
    </style>

    <style name="Theme.AndroidApp" parent="Base.Theme.AndroidApp" />
</resources>